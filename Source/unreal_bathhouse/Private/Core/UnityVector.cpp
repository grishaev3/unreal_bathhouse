#include "UnityVector.h"

// --- ОПРЕДЕЛЕНИЕ И ИНИЦИАЛИЗАЦИЯ КОНСТАНТЫ ---
// Создает в памяти неизменяемый вектор со значениями (0, 0, 0)
const FUnityVector FUnityVector::ZeroVector = FUnityVector(0.0f, 0.0f, 0.0f);
